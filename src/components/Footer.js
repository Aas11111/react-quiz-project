function Footer({ children }) {
  return (
    <footer
    // style={{
    //   display: "flex",
    //   alignContent: "center",
    //   justifyContent: "center",
    // }}
    >
      {children}
    </footer>
  );
}

export default Footer;
